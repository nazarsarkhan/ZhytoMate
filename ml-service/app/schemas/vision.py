"""
Purpose:   VisionRequest / VisionResponse models, CategoryEnum, severity 1..5 constraint.
Layer:     schema
May import:   stdlib, pydantic, schemas/common (category enum)
Must NOT import:  api/*, services/*, components/*, domain/*; any I/O or model lib (asyncpg, google-genai, sentence-transformers, FastAPI routing)
"""
