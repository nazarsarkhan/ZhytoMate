# Dockerfile — builds the ML service image; pre-downloads the e5 model into a build
# layer so the demo never fetches ~1.1 GB at runtime (docs/SYSTEM_DESIGN.md §9).
# TODO: base image, deps install, model pre-download, non-root user, uvicorn entrypoint.
