# .env.example — template of required env vars with NO real values (copy to .env locally).
# .env itself is git-ignored and never committed (docs/SYSTEM_DESIGN.md §8).
# TODO: list keys only — DATABASE_URL, GEMINI_API_KEY, INTERNAL_TOKEN, GEMINI_MODEL,
# EMBED_MODEL, SIM_GATE, SIM_HIGH, EF_SEARCH, rate limits, timeouts, pool sizes.
